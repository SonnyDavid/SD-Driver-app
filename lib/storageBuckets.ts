/** Supabase Storage bucket for delivery proof photos (public read). */
export const DELIVERY_PHOTOS_BUCKET = "delivery-photos";

/** Supabase Storage bucket for driver profile photos (public read). */
export const DRIVER_PROFILE_BUCKET = "driver-profiles";
